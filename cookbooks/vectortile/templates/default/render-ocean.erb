#!/bin/sh
set -eu
export PGAPPNAME='render-ocean'

<%= @tilekiln_bin %> generate zooms \
--source-dbname "<%= @source_database %>" \
--storage-dbname "<%= @storage_database %>" \
--num-threads "<%= node[:vectortile][:rerender][:lowzoom][:threads] %>" \
--config <%= @config_path %> \
--min-zoom <%= @min_zoom %> \
--max-zoom <%= @max_zoom %>
